# Live updates now use polling + Turbo morph, so this transport is unused. Kept as an
# inert in-process async adapter so the framework still boots cleanly.
development:
  adapter: async

test:
  adapter: async

production:
  adapter: async
