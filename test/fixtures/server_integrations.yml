jait_one:
  server: one
  kind: jait
  enabled: true
  api_token: test-token-1
  base_url: https://justanotherissuetracker.com
  # jsonb column — a quoted JSON string round-trips as a String, not an Array of team hashes.
  discovered_teams:
    - id: 21
      identifier: HOUR
      name: Hourglass
  webhook_secret: test-webhook-secret
